import React from "react";
import "./Button.css";

const Button = (props) => {
  //const { time, isTaken } = props;
  return <button className="time-button">{"time"}</button>;
};

export default Button;
