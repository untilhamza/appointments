import React from "react";
import PhoneForm from "../components/PhoneForm";

const CheckAppointment = () => {
  return (
    <div>
      <PhoneForm onConfirm={() => {}} onCancel={() => {}} />
    </div>
  );
};

export default CheckAppointment;
